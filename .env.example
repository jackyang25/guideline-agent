# Copy to .env and fill in your OpenAI key (no quotes, no export). .env is git-ignored.
OPENAI_API_KEY=sk-REPLACE_ME
OPENAI_MODEL=gpt-5.5
# Optional: where extractions are written (default: ./guidelines)
# GE_OUTPUT_ROOT=./guidelines
